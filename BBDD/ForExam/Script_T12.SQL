DROP DATABASE IF EXISTS p1;

CREATE DATABASE p1;

USE p1

CREATE TABLE ALUMNOS (
DNI VARCHAR(10) ,
NUMEXP VARCHAR(14) UNIQUE NOT NULL,
NOMBRE VARCHAR(14) NOT NULL,
TLF NUMERIC UNIQUE,
CONSTRAINT PK_ALUMNOS PRIMARY KEY (DNI));

CREATE TABLE ORDENADORES (
NUMSERIE NUMERIC,
RAM NUMERIC NOT NULL,
DNIALUMNO VARCHAR (10) NOT NULL,
CONSTRAINT PK_ORDENADORES PRIMARY KEY (NUMSERIE));

ALTER TABLE ORDENADORES ADD
CONSTRAINT FK_ORDENADORES FOREIGN KEY (DNIALUMNO) REFERENCES ALUMNOS (DNI);